---
import PlanCard from './PlanCard.astro';
import { asset } from '../utils/assets';

const originalPlans = [
  {
    title: 'Santurban',
    price: '$290.000',
    image: asset('/images/cabaña.jpeg'),
    description: 'Una experiencia inolvidable, cabaña con calefacción y desayuno incluido.',
    features: ['wifi', 'jacuzzi', 'desayuno'],
  },
  {
    title: 'Plan Romántico',
    price: '$390.000',
    image: asset('/images/exterior.jpeg'),
    description: 'Disfruta de una agradable decoración, desayuno, almuerzo y fogata con chocolate.',
    features: ['wifi', 'desayuno', 'fogata', 'almuerzo', 'decoración'],
  },
  {
    title: 'Plan Laguna Encantada',
    price: '$560.000',
    image: asset('/images/paisaje.jpeg'),
    description: 'Una experiencia frente a la laguna con una vista hermosa.',
    features: ['wifi', 'Laguna', 'desayuno'],
  },
  {
    title: 'Plan Páramos',
    price: '$230.000',
    image: asset('/images/cama.jpeg'),
    description: 'Disfruta un día de descanso en nuestra cabaña con una vista hermosa.',
    features: ['wifi', 'desayuno', 'fogata'],
  },
  {
    title: 'Plan Noche de Pareja',
    price: '$490.000',
    image: asset('/images/espejo.jpeg'),
    description: 'Disfruta una velada romántica con la persona que amas.',
    features: ['wifi', 'desayuno', 'vino', 'Fogata', 'Almuerzo'],
  },
];

// Triple the plans for robust infinite effect (Set 1 + Set 2 + Set 3)
const plans = [...originalPlans, ...originalPlans, ...originalPlans];
const originalCount = originalPlans.length;
---

<section id="planes" class="relative overflow-hidden bg-[#071412] py-24">
  <!-- Subtle patterns -->
  <div class="pointer-events-none absolute inset-0 opacity-5">
    <div class="bg-primary absolute top-0 right-0 h-96 w-96 rounded-full blur-[120px]"></div>
    <div class="bg-primary absolute bottom-0 left-0 h-96 w-96 rounded-full blur-[120px]"></div>
  </div>

  <div class="relative z-10 container mx-auto px-4 md:px-6">
    <div class="mb-16 flex flex-col justify-between gap-6 md:flex-row md:items-end">
      <div class="text-left">
        <p class="text-primary mb-3 text-[10px] font-bold tracking-[0.4em] uppercase md:text-xs">
          ¿Qué experiencia te gustaría vivir?
        </p>
        <h2
          class="font-serif text-3xl font-bold tracking-[0.15em] text-white uppercase md:text-4xl"
        >
          Nuestros Planes
        </h2>
      </div>
    </div>

    <!-- Carousel Container -->
    <div class="group relative">
      <!-- Navigation Buttons -->
      <button
        id="prev-plan"
        class="hover:bg-primary hover:border-primary absolute top-[40%] -left-4 z-20 flex h-12 w-12 -translate-y-1/2 items-center justify-center rounded-full border border-white/20 bg-white/10 text-white backdrop-blur-md transition-all active:scale-90 md:-left-8"
        aria-label="Anterior"
      >
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="24"
          height="24"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"><path d="m15 18-6-6 6-6"></path></svg
        >
      </button>

      <button
        id="next-plan"
        class="hover:bg-primary hover:border-primary absolute top-[40%] -right-4 z-20 flex h-12 w-12 -translate-y-1/2 items-center justify-center rounded-full border border-white/20 bg-white/10 text-white backdrop-blur-md transition-all active:scale-90 md:-right-8"
        aria-label="Siguiente"
      >
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="24"
          height="24"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"><path d="m9 18 6-6-6-6"></path></svg
        >
      </button>

      <div
        id="plans-carousel"
        class="scrollbar-hide flex cursor-grab snap-x snap-mandatory gap-6 overflow-x-auto pb-12 select-none active:cursor-grabbing"
      >
        {
          plans.map((plan, index) => (
            <div class="plan-item pointer-events-none min-w-[85vw] snap-center md:min-w-[420px]">
              <div class="pointer-events-auto">
                <PlanCard {...plan} />
              </div>
            </div>
          ))
        }
      </div>

      <!-- Scroll Progress Bar -->
      <div class="mt-4 h-0.5 w-full overflow-hidden rounded-full bg-white/10">
        <div id="carousel-progress" class="bg-primary h-full w-0 transition-all duration-300"></div>
      </div>
    </div>

    <!-- Menu Button -->
    <div class="mt-20 flex justify-center">
      <a
        href="#menu"
        class="group hover:border-primary/50 relative inline-flex items-center justify-center overflow-hidden rounded-full border border-white/20 bg-transparent px-10 py-4 font-bold transition-all"
      >
        <span
          class="bg-primary absolute inset-0 h-full w-0 transition-all duration-300 ease-out group-hover:w-full"
        ></span>
        <span class="relative text-xs tracking-widest text-white uppercase group-hover:text-white"
          >Mirar nuestro menú</span
        >
      </a>
    </div>
  </div>
</section>

<style>
  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }
  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

  #plans-carousel {
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch;
  }

  #plans-carousel.dragging {
    scroll-behavior: auto;
    scroll-snap-type: none;
  }
</style>

<script define:vars={{ originalCount }}>
  const carousel = document.getElementById('plans-carousel');
  const progress = document.getElementById('carousel-progress');

  if (carousel && progress) {
    let items = carousel.querySelectorAll('.plan-item');
    let setWidth = 0;
    let isDown = false;
    let startX;
    let scrollLeft;
    let autoScrollActive = true;

    const updateDimensions = () => {
      const firstItem = items[0];
      const secondItem = items[1];
      if (!firstItem) return;
      const itemWidth = firstItem.clientWidth;
      const gap = secondItem
        ? secondItem.offsetLeft - (firstItem.offsetLeft + firstItem.clientWidth)
        : 24;
      setWidth = (itemWidth + gap) * originalCount;
    };

    // Use a small delay or check for first item size
    const init = () => {
      updateDimensions();
      carousel.scrollLeft = setWidth;
    };

    // Wait for everything to load properly
    window.addEventListener('load', init);
    // Also try immediately
    setTimeout(init, 500);

    // Infinite Logic
    carousel.addEventListener('scroll', () => {
      if (setWidth === 0) return;

      const scrollPos = carousel.scrollLeft;

      // Progress Bar (based on middle set)
      const normalizedScroll = scrollPos % setWidth;
      const scrollPercentage = (normalizedScroll / setWidth) * 100;
      progress.style.width = `${scrollPercentage}%`;

      // Seamless Jump Logic - Only jump if not dragging to avoid glitches
      if (!isDown) {
        if (scrollPos < setWidth * 0.5) {
          carousel.scrollTo({ left: scrollPos + setWidth, behavior: 'instant' });
        } else if (scrollPos > setWidth * 2.2) {
          carousel.scrollTo({ left: scrollPos - setWidth, behavior: 'instant' });
        }
      }
    });

    // Auto-scroll logic with RequestAnimationFrame (Smoother)
    let lastTime = 0;
    const animate = (time) => {
      if (autoScrollActive && !isDown && setWidth > 0) {
        carousel.scrollLeft += 0.5;
      }
      requestAnimationFrame(animate);
    };
    requestAnimationFrame(animate);

    // Interaction handling
    const stopAuto = () => {
      autoScrollActive = false;
    };
    const startAuto = () => {
      autoScrollActive = true;
    };

    carousel.addEventListener('mousedown', (e) => {
      isDown = true;
      stopAuto();
      carousel.classList.add('dragging');
      startX = e.pageX - carousel.offsetLeft;
      scrollLeft = carousel.scrollLeft;
    });

    window.addEventListener('mouseup', () => {
      if (!isDown) return;
      isDown = false;
      carousel.classList.remove('dragging');

      // Perform infinite jump after release if needed
      const scrollPos = carousel.scrollLeft;
      if (scrollPos < setWidth * 0.5) {
        carousel.scrollTo({ left: scrollPos + setWidth, behavior: 'instant' });
      } else if (scrollPos > setWidth * 2.2) {
        carousel.scrollTo({ left: scrollPos - setWidth, behavior: 'instant' });
      }

      setTimeout(startAuto, 2000);
    });

    carousel.addEventListener('mousemove', (e) => {
      if (!isDown) return;
      e.preventDefault();
      const x = e.pageX - carousel.offsetLeft;
      const walk = (x - startX) * 2;
      carousel.scrollLeft = scrollLeft - walk;
    });

    carousel.addEventListener('mouseenter', stopAuto);
    carousel.addEventListener('mouseleave', () => {
      if (!isDown) startAuto();
    });
    carousel.addEventListener('touchstart', stopAuto);
    carousel.addEventListener('touchend', startAuto);

    // Buttons
    document.getElementById('prev-plan')?.addEventListener('click', () => {
      stopAuto();
      const step = items[0].clientWidth + 24;
      carousel.scrollBy({ left: -step, behavior: 'smooth' });
      setTimeout(startAuto, 3000);
    });

    document.getElementById('next-plan')?.addEventListener('click', () => {
      stopAuto();
      const step = items[0].clientWidth + 24;
      carousel.scrollBy({ left: step, behavior: 'smooth' });
      setTimeout(startAuto, 3000);
    });

    window.addEventListener('resize', updateDimensions);
  }
</script>
